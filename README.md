# Ember Northwest site update

Updates included:
- Restored the scrolling brand logo marquee using SVGs from `public/assets/brand`.
- Kept the ember cursor glow active.
- Kept enlarged hero stats.
- Verified production build after refreshing dependencies.

Run locally:

```bash
npm install
npm run dev
```
